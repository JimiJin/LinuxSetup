user www-data;
worker_processes auto;
pid /run/nginx.pid;

events {
    worker_connections 768;
    # multi_accept on;
}

http {

    server {

        location / {
            root /root/webui-aria2/docs;
        }

        #listen 80;
        #server_name 38.143.8.143;
        
        #location /aria2/download {
        #    alias /root/Download;
        #    autoindex on;
        #    autoindex_exact_size off;
        #    autoindex_localtime on; 
        #    charset utf-8,gbk;
        #}
        
        #location /aria2/home {
        #    index index.html
        #    charset utf-8,gbk;
        #}
    }
}